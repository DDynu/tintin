services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: tintin
      POSTGRES_PASSWORD: tintin
      POSTGRES_DB: tintin
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
